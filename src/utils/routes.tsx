export enum Routes {
  INDEX = "/",
  LOGIN = "/login",
  REGISTER = "/register",
  TRANSCRIBING = "/transcribing",
  RESOURCES = "/resources",
  COMMUNITIES = "/communities",
  SETTINGS = "/settings",
  // Admin Role routes
  TOPIC = "/topics",
}
